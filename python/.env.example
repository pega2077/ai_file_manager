# AI File Manager 配置模板
# 复制此文件为 .env 并根据实际情况修改配置

# =================================
# 服务配置
# =================================
HOST=127.0.0.1
PORT=8000
DEBUG=true

# =================================
# 文档处理配置
# =================================
CHUNK_SIZE=1000
CHUNK_OVERLAP=200
MAX_FILE_SIZE_MB=50
# Pandoc 可执行文件路径，相对于项目根目录
PANDOC_PATH=bin/pandoc.exe

# =================================
# Embedding 配置
# =================================
# 支持的模型：sentence-transformers/all-MiniLM-L6-v2, all-mpnet-base-v2 等
EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2
EMBEDDING_DIMENSION=384

# =================================
# 搜索配置
# =================================
SIMILARITY_THRESHOLD=0.7
MAX_SEARCH_RESULTS=20

# =================================
# LLM 配置
# =================================
# LLM 类型：local, openai, ollama, claude, custom
LLM_TYPE=local
LLM_MODEL=
# Ollama 等本地 LLM 端点
LLM_ENDPOINT=http://localhost:11434
# OpenAI, Claude 等 API 密钥（如果使用）
LLM_API_KEY=
LLM_TEMPERATURE=0.7
LLM_MAX_TOKENS=1000

# =================================
# 路径配置（可选）
# =================================
# 以下路径如果不设置，会自动使用项目目录下的默认路径
# DATABASE_PATH=database
# WORKDIR_PATH=workdir
# LOGS_PATH=logs
